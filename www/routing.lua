-----------------------------------------------
-----------------------------------------------

map('^/devices/(.*)', 'device/detail')
map('^/data/(.*)', 'device/data')
map('^/users/(.*)', 'user/detail')
